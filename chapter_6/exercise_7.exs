IO.inspect List.to_string(:io_lib.format("~.2f",[10.28]))
System.get_env("PATH")
Path.extname("foo.exs") # Return extension
System.cwd # Current working dirrectory
System.cmd("whoami", []) # Current running process
# library to convert JSON Strings into elixir data structures
# https://github.com/cblage/elixir-json
